(ns test-direct-linking.test-direct-linking-test
  (:require [clojure.test :refer :all]
            [test-direct-linking.test-direct-linking :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
